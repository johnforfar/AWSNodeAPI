//buildspec.yml
version: 0.420
phases:
    install:
      runtime-versions:
      nodejs: 18
    commands:
      - 'npm install'
    build:
        commands:
            - 'npm run build'
artifacts:
  files:
    - package.json
    - package-lock.json
    - '**/*'
    - README.md